# Day 3 — Functions and String Methods

## Concepts learned
- def keyword — defining a function
- Parameters vs arguments — placeholder vs real value
- return vs print — return gives value back, print just displays
- Local vs global scope — variables inside functions stay inside
- Single responsibility — each function does one job
- Method chaining — name.strip().lower()
- String methods: strip, lower, upper, split, join, replace, find, len
- append() — adds item to end of a list

## What pseudocode means and why I use it first
means writing how the program will work in simple english step by step.

## What was hard today
logic building was hard and remembering the strings functions.

## What clicked today
----

## Questions I still have
how to improve building logic.